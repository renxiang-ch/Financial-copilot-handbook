# 06 · Product Track (Optional)

Covers `frontend.py`, `views/*.py`, `dashboard.py`, and the `/dashboard/*`
half of `api.py`. Chat remains the primary entry point; this is the secondary
one.

<!-- TODO: three views (Company Exposure, Supplier Deep Dive, What-if), each
     zero-LLM-cost pure SQL. The What-if view's threshold_only floor-estimate
     separation is worth a dedicated subsection -- it is the strongest example
     in the whole product of a UI actively preventing a specific,
     documented misreading. -->
