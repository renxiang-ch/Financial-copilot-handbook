# Appendix C · Cost & Time Budget

<!-- TODO:
- Embedding generation: CPU time, one-off
- Full eval_set.json run: token cost + wall time (pull from a real
  eval_results_*.json's "estimated_cost_usd" / "total_latency_s", don't
  estimate)
- Tier-3 run: same
- Multi-turn set with --repeat 5: same, and note it costs ~5x a single run,
  which is why it is not run casually
- A one-line total for "clone to first successful eval run"
-->
